# Every game the project builds gets one line here. Each game is its own
# subfolder with its own CMakeLists.txt (see source/games/buz_game).
ADD_SUBDIRECTORY(simple_game)  # the small starter game, and the default startup project
ADD_SUBDIRECTORY(buz_game)     # the feature-touring demo
